/* Тестовый файл для проверки компилятора с 8051 кодом */

// Объявление специальных регистров
sfr P1 = 0x90;

// Объявление битов
sbit P1_0 = 0x90;

// Основная функция
void main(void) {
	P1 = 0x01;  // Записываем значение в порт P1
	while(1);   // Бесконечный цикл
} 